Dependency:

    python-kqt4
    python-kde4

Install timekpr:

    Add ppa:timekpr-maintainers/ppa to your repository
    sudo apt-get install timekpr

Install:

  cd kcm_timekpr/src
  mkdir build
  cd build
  cmake .. -DCMAKE_INSTALL_PREFIX=`kde4-config --prefix`
  make
  sudo make install

Uninstall:

  cd kcm_timekpr/src/build
  sudo make uninstall